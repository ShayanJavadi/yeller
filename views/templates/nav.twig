<nav class="navbar navbar-default navbar-fixed-top">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <div class="whitetext">
              <a class="navbar-brand " href="{{ path_for('home')}}"><strong>YELLER</strong></a>
            </div>
        </div>
        <div id="navbar" class="collapse navbar-collapse">
          <ul class="nav navbar-nav navbar-right searchBar">
          </ul>
            <ul class="nav navbar-nav navbar-right">
              <li><form class="navbar-form navbar-right searchBar" action="{{ path_for('search')}}">
              <div class="input-group">
                  <input type="text" class="form-control customSearchInput" placeholder="Search" name="searchInput">
                  <div class="input-group-btn">
                    <select class="form-control customSearchOptions" name="searchType">
                      <option value="user">User</option>

                    </select>
                      <button class="btn btn-default customSearchButton" type="submit"><i class="glyphicon glyphicon-search customSearchGlyph"></i></button>
                  </div>
              </div>
              </form></li>
              {% if auth.isLoggedIn %}
                    <li><a href="{{ path_for('auth.signout')}}">Sign Out</a></li>
                    <li><a href="{{ path_for('home')}}">{{ auth.user.name }}</a></li>
              {% else %}
                    <li><a href="{{ path_for('auth.signup')}}">Sign Up</a></li>
                    <li><a href="{{ path_for('auth.signin')}}">Sign In</a></li>
              {% endif %}
                    <li><a href="{{ path_for('home')}}">Home</a></li>
                    <li><a href="{{ path_for('contact' )}}">Contact</a></li>
            </ul>
        </div>
    </div>
</nav>
